# Test Database Configuration
spring:
  datasource:
    hikari:
      minimum-idle: 1
      maximum-pool-size: 3
  flyway:
    enabled: true

logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: WARN